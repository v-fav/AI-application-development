# ПР5. Семантичний пошук у колекції документів

# Локальні моделі ембедінгів. Разом із пакетом ставиться torch —
# це кілька сотень мегабайт; для цієї роботи достатньо версії для CPU.
sentence-transformers>=3.0

# Вектори, схожість, збереження індексу
numpy>=1.26

# Пошук за ключовими словами (BM25) для порівняння. Якщо ранжуєте
# по-своєму, рядок можна прибрати
rank_bm25>=0.2

# Читання налаштувань із .env
python-dotenv>=1.0

# Веб-рівень
fastapi>=0.115
uvicorn[standard]>=0.30

# Потрібен, лише якщо ембедінги рахуєте через API (ключ із ПР3),
# а не локальною моделлю. Тоді sentence-transformers можна прибрати.
# openai>=1.40

# Потрібен, лише якщо сторінку рендеритимете шаблоном.
# Спосіб віддати сторінку — на ваш вибір, тож рядок можна прибрати.
jinja2>=3.1
